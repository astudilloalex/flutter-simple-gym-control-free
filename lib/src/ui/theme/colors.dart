import 'package:flutter/material.dart';

class DarkColors {
  const DarkColors._();

  static const Color primary = Color(0xFF222831);
  static const Color primaryVariant = Color(0xFF393E46);
  static const Color secondary = Color(0xFF00ADB5);
  static const Color secondaryVariant = Color(0xFFEEEEEE);
}

class LightColors {
  const LightColors._();

  static const Color primary = Color(0xFF21ABCD);
  static const Color primaryVariant = Color(0xFFE1EEFF);
  static const Color secondary = Color(0xFFC2CFD8);
  static const Color secondaryVariant = Color(0xFF543A3A);
}
